services:
  - type: web
    name: fulltask-ai
    env: node
    buildCommand: |
      cd client
      npm install
      npm run build
      cd ..
      npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
