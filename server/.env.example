# Put these as Environment Variables on Render (do NOT commit real secrets)
OPENAI_API_KEY=sk-...
SERVICE_ACCOUNT_JSON= (the full JSON of your service account, compressed into one line OR use Render file secret)
PORT=8877
ALLOWED_ORIGIN=http://localhost:5173
